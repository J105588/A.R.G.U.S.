mitmproxy>=10.0.0
Flask>=2.3.0
Flask-SocketIO>=5.3.0
python-dateutil>=2.8.2
requests>=2.31.0
# The following are often bundled with Python or mitmproxy, but listed for clarity
# sqlite3 is a built-in module
# psutil and winreg are not directly required by the provided code but can be useful